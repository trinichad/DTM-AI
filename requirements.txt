# DTM AI backend — runtime deps for the Ubuntu server (Python 3.11+).
# The SECURE CORE (config, credentials, registry, dispatch, router, audit, validation)
# is stdlib-only and runs/tests with NONE of these installed — so the guardrails are
# verifiable anywhere. These are needed only for the web API + live integrations.

fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27              # vendor API clients (replaces requests)
PyJWT>=2.8              # Cylance JWT (replaces the hand-rolled HS256 — see findings.md)
passlib[bcrypt]>=1.7    # password hashing
itsdangerous>=2.1       # signed sessions
python-dotenv>=1.0      # .env loading
SQLAlchemy>=2.0         # DB layer (sqlite dev / postgres prod)
psycopg[binary]>=3.1    # postgres driver (prod)
