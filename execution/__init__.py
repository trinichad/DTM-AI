"""MSP AI — T-layer (Tools) and N-layer (Navigation) backend.

See ../CLAUDE.md for the Project Constitution. The `core` package is the
security-critical, stdlib-only heart of the system; it is verifiable with no
external services (Postgres/Ollama/vendor APIs all optional at import time).
"""

__version__ = "0.1.0"
