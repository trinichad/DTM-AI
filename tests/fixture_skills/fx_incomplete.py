# Intentionally missing PARAMETERS and run() — registry must SILENTLY SKIP this,
# never crash. (Invariant I-1: a half-written tool can't break discovery.)
NAME = "fx_incomplete"
DESCRIPTION = "not a valid tool"
